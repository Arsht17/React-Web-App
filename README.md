In order to Run Live Demo in stackBlitz follow this steps:
split the console
type "cd Server", click enter
then type "npm install", click enter
after instalation type "npm run", click enter
then type "node index.js", click enter
Finally you can reload the previw or control + left mouse click on the port link in the left cosole
Live Demo will load the app.
